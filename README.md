# YUON

## TODO

- 채팅/문서 통계 인메모리 저장소를 영속 저장소(예: DB)로 교체해 대시보드 통계가 재시작 시에도 유지되도록 개선
- PDF 추출 시 `pdfcpu.ExtractTextFile`로 교체하거나 추출 실패 시 대체 경로(fallback) 추가해 텍스트 추출 정확도 향상
- 인증/회원 관리 영속화: 현재 메모리 기반 사용자 저장소로 로그인/회원가입 정보가 유지되지 않음. DB에 사용자/권한 저장 및 JWT 검증 연동, FE 로그인 후 관리자 리다이렉션 정상 동작 확인 필요
- 관리자 대화 내역 페이지: 현재 목데이터 기반 UI만 존재. 대화 로그 조회/검색 API 서버 구현 및 FE 연동 필요
- 사용자 관리 페이지: 목데이터 기반. 사용자 CRUD/RBAC API 추가 및 FE 연동 필요
- DB 마이그레이션 도구 설정: Makefile에 TODO로 남은 migrate-up/down 스텁 구현 필요
- 분석 키워드 토큰화 개선: 현재 단순 문자열 분리로 “니다.” 등 불필요 토큰이 집계됨. LLM(OpenAI 등) 기반 키워드 추출로 교체해 한국어/영문 키워드 통계 품질 개선
